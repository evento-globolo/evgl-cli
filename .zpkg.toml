[package]
org = "evento-globolo"
name = "evgl-cli"
version = "0.1.0"
description = "Command-line interface for Evento Globolo"
license = "MIT"
language = "rust"

[package.repository]
vcs = "git"
url = "https://github.com/evento-globolo/evgl-cli"

[publish]
include_readme = true
tag_format = "v{version}"
exclude = [".env", ".env.*", "node_modules/**", "zed_modules/**", "target/**", "dist/**", "*.log"]

[dependencies]
"evento-globolo/evgl-clients" = "^0.1.0"
"evento-globolo/evgl-interfaces" = "^0.1.0"
"evento-globolo/evgl-libs" = "^0.1.0"

[install]
dir = "zed_modules"
adapter = "rust"
